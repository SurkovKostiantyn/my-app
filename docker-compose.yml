  services:
    app:
      build: ..
      command: npm run dev
      volumes:
        - .:/usr/app
      ports:
        - "3000:3000"
      restart: unless-stopped
      environment:
        - NODE_ENV=development
        - PORT=3000
